3.2.1 — Fix duplicate update hook

- Merge pacing-context refresh and repeated-warning flushing into one PacingManager.update hook. Fix the DMF rehook warning and restore per-update context refresh.
- Enforce DMF's one-hook-per-mod/function registration rule in the test harness, including mixed hook and hook_safe registrations. Reproduce the previous package's conflict and verify the merged callback.
- Keep intensity values, compact UI and spawn smoothing unchanged. Fully exit the game before replacing HCM; existing settings and HED compatibility are retained.

3.2.0 — Sustained pressure and smoother spawning

- Prioritize overall intensity and collapse all 18 individual multipliers into Advanced settings. Keep the main page concise and explanations in hover help.
- Strengthen the upper levels: level 10 uses 6× ordinary horde waves, 1.35× wave size, 2.25× trigger frequency, 3× specialist slots and 3.25× replenishment frequency, with increased patrol and map monster requests. Retain wave spacing and native capacity constraints.
- Admit at most two due native spawners per update, fairly deferring the others without dequeuing their enemies. Preserve queue IDs, enemy order, objective parameters and completion callbacks.
- Defer ordinary/coordinated horde waves when the central pending queue reaches 48; native retries preserve unsent waves. Leave trickle attempts unchanged.
- Summarize repeated Buff-capacity warnings to reduce log writes. Keep the native pool and overflow rejection unchanged; flush remaining counts and release session references on exit.
- Remove duplicate template discovery, copy each modified compiler branch once and restrict per-frame hover checks to relevant controls.
- Retain spawn-ring growth, deferred map encounter preservation and temporary-vector fixes. Validate with actual game Lua and three-language layout checks; no live FPS or difficulty-multiplier measurement is claimed.
- Compatible with Havoc Enemy Director 2.0.0. Fully exit the game before replacing HCM, then re-select an intensity level. Existing values are preserved until changed.

3.1.0 — Coordinated intensity and spawn integrity

- Replace the all-equal overall multiplier with coordinated intensity levels 1–10. Levels emphasize elite patrols, specialist reinforcements and map monster encounters through sustained waves.
- Keep ordinary enemy density, enemies per wave, ordinary horde frequency, event budgets and escalation at baseline in the profile. Increase wave counts and specialist scheduling moderately; retain wave spacing and concurrent-enemy limits.
- Preserve existing saved values. Re-select an intensity level to replace old combined multipliers; individual controls retain their 10x ceiling and HED overrides remain available.
- Fix a reproduced overflow in the 256-entry minion spawn ring. Grow the existing ring before an unread request is overwritten, preserving request order, positions and attached event/objective parameters. Continue processing one request per update.
- Fix unspawned map enemies being deleted when pacing temporarily refuses activation. Modified configurations retain these plans and retry within the normal update budget. Active and dead-unit cleanup remains intact.
- Reproduce both failure paths using the game's Lua. Verify large wrapped queues, callback-driven growth, deferred elite recovery, cleanup, profile mappings and three-language UI layouts offline.
- Compatible with Havoc Enemy Director 2.0.0. Replace HCM and fully restart the game. Live in-game confirmation is still needed.

3.0.1 — Sliders, clearer help and high-density placement fix

- Replace multiplier step buttons with sliders, narrow title columns and remove the separate reset controls.
- Add Overall intensity to set all 18 multipliers together; raise every multiplier ceiling to 10x.
- Show mixed individual settings as Custom and retain direct numeric entry.
- Move detailed explanations into hover panels. Define combat load, specialist slots, coordinated tactics, recovery and escalation; remove arithmetic-only examples in all three languages.
- Clarify that recovery may end before its timer when tension falls.
- Reclaim temporary vectors after each map enemy placement group while preserving boxed results, enemy counts and random calls. This targets the engine temporary-marker failure reported during high-density map loading; the engine crash still needs live confirmation.
- Check the actual game slider mouse logic at three UI scales and verify every hover panel in English, Simplified Chinese and Traditional Chinese.
- Compatible with Havoc Enemy Director 2.0.0. Fully restart the game after replacing HCM.
- No live in-game playtest is included; the high-density regression uses the game Lua with explicit engine boundaries.

3.0.0 — Native template redesign

- Import eligible condition rows from SoloPlay without registering separate Havoc or Maelstrom definitions.
- Replace category multipliers with 18 controls across six native pacing groups.
- Share the native-template registry and exact-field pipeline with Havoc Enemy Director.
- Retain native scheduling, encounter budgets, shared-mutator deduplication and mission-owned cleanup.
- Migrate valid condition IDs while preserving the previous saved value; old multiplier modes start from new defaults.
- Save edits for the next mission and retain the running mission's initial configuration.
- Provide English, Simplified Chinese and Traditional Chinese controls and installation documents.
- Completely remove diagnostics, sampling hooks, switches, translations and diagnostic modules.
- Validate Lua syntax/imports, native-manager behavior, input controls and package installation layout offline. No live in-game playtest or measured FPS claim is included.
- Install current companion versions and fully restart the game. For manual upgrades, remove the old mod installation folder first.
