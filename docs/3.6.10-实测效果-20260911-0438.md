# 3.6.10 实测：补投约 98%，地形与门校验已生效

本次日志为 `console-2026-09-10-20.25.35-534262d9-4361-41db-b7fa-4dddf0fb3446.log`，2026 年 9 月 11 日北京时间约 04:26:35–04:38:11 的法庭密牢，任务胜利。日志确认加载 3.6.10，安装目录 45 个运行文件也与该版本 ZIP 一致。

本轮修改已经在实机体现效果：补投从上一局 119 / 210 提高到本局 200 / 204，任务内完成了全部计划中的后补遭遇。仍有原版导航卡住记录，不能把“补投创建成功”当作“地图上所有敌人都能顺利接敌”。本次仅分析日志，继续保留现有有界诊断，没有修改源码或另发版本。

## 与上一局比较

| 指标 | 上一局 3.6.9 | 本局 3.6.10 |
| --- | ---: | ---: |
| 实际回收 | 225 | 220 |
| 有效补投额度 | 210 | 204 |
| 实际补回 | 119 | 200 |
| 补回／有效额度 | 56.7% | **98.0%** |
| 结束时仍待补 | 91 | **4** |
| 额度丢弃 | 0 | 0 |
| 待补额度峰值 | 98 / 256 | 36 / 256 |
| 已完成补投批次的最大年龄 | 190.2 秒 | 38.8 秒 |
| 选点完整寻路查询 | 143 | 71 |
| 原生候选查询 | 158 | 132 |
| 局部导航投影采样 | 552 | 416 |
| 后补怪物完成／计划 | 4 / 4 | 9 / 9 |
| 后补巡逻完成／计划 | 5 / 9 | 4 / 4 |
| 原版明确卡死移除 | 2 | 1 |

两局的时长、遭遇计划和战斗过程不同，不能把数值差异全部归因于本次改动，也不能换算为帧率提升。不过，“大量额度结束前没补入”在本局已明显缓解。查询次数较少，日志没有显示本次开放地形导致选点查询增多。

本局 204 = 200 + 4，所有接受的额度均有去向。200 个入队请求全部实际创建，没有出队取消。220 次回收比有效额度多出的 16 次，对应已经补投过的单位再次被回收，按规则不再生成新额度；这不等于可以证明这 16 个单位出生时就卡住。

补投受阻采样为：原版调度暂停 83、无可用点 56、等待路径 28、容量限制 27、路线失效 17、位置不满足条件 4。它们是采样次数，不是等待秒数或受阻敌人数；日志不能逐一解释最后四个额度为何未用完。

## 地形放行与门检查

本局共用选点回执记录：

- 含地形连接的路线通过 **33 次**。
- 含门连接的路线通过 **4 次**。
- 门路线在初次检查被拒绝 0 次，已有路线的门状态复查拒绝 **1 次**。
- 未知连接 0 次、原生无路 16 次、附近路线通过 29 次、过时结果 5 次。

以上按路径检查计数，不是 33 个地点、4 扇门或对应数量的敌人。同一条路径可能同时包含地形和门，计数可以重叠；该模块同时服务额外巡逻、遭遇后补和回收补投，不能全部归为回收队列。

04:33:48.519，门连接 **595** 的复查出现 `door_blocked`。候选点为 `(-206.27, -48.7825, 21.9999)`。在当前代码中，这表示门口导航仍被阻挡，并且原生 AI 开门检查不允许开启；已有路线许可因此失效。这是门检查在实机拦住不可用路线的直接证据，不能进一步把该门断定为“玩家专用门”。

此前担心的“寻路结果看似成功，但门状态不允许通过”确实需要独立复查。另一方面，四次含门路线通过说明没有继续把所有门一律拒绝。

## 旋转楼梯与粉碎者

旧问题候选 `(37.9879, -86.1169, 16.1507)` 分别在 **04:30:11.915、04:30:44.077** 被判为 `unreachable`。地形已经开放，这两次仍是原生查询没有找到路径，不能归因于旧版统一拒绝特殊连接。它们没有获得生成许可。

本局第 27 批为 21 只粉碎者，在 `(-52.82, -78.3433, 14.0934)` 完整创建，13.59 秒完成，21 个成员均使用附近候选，局末该批次回收数为 0。其出生点与旧问题点不同。日志能证明创建与回收记录，不能由此逐只证明它们都接战或被击杀。

楼梯区域仍有一条原版 `MINION STUCK DESPAWN`：**04:31:17.580**，机枪兵从 `(118.817, -136.936, -1.86907)` 前往 `(100.542, -130.498, 11.9914)` 时被判卡死并移除。缺少 HCM 批次对应，无法确定其来自原生出生还是本 mod 的额外生成。

本局原版智能连接退出失败记录共 265 条，其中门 22、台沿 134、带围栏台沿 36、掩体台沿 22、翻越 51。它们不等于 265 个独立敌人卡死，部分可能反复尝试后恢复；但说明放开地形并没有消除原版动作执行问题。也没有证据把这些记录全部归因于 HCM。

## 其他效果与限制

7 队路线巡逻、4 队后补巡逻全部创建完成，共 382 人，其中精英 348、使用附近点 236。它们后来被 HCM 回收的成员合计 5；上一局对应额外巡逻合计回收 34 人。批次与战斗过程不同，不能据此给出精确的接敌成功率。本局没有“巡逻部署停止仍欠成员”的回执，后补储备在 04:34:21 已全部用完。

回收就绪队列峰值 38 / 64，满队列采样 0，结束时就绪引用 0。成功回收在基本 30 秒之外的最大额外等待为 13.1 秒，上一局为 11.7 秒；没有出现满队列拥堵，但这项时间没有变短。未接战后方散怪回收仍为 0，尚无该分支成功触发的实机证据。

20 条有限回收诊断中，17 条因离队友不到 35 米而保留，2 条因目标可见，1 条为被动散怪不满足后方进度条件。该散怪虽然距队友 49.6 米，但后方进度差为 -32.2 米，不满足需要落后 60 米的条件。不能把这些保留样本当成回收程序漏处理，也不能由“就绪引用 0”推断全图没有剩余敌人。

本局实际生成普通兵 2271、精英 1225、特感 389、怪物 26、其他 4，共 3915。上一局为 4363，生成总量减少，但本局任务较短且遭遇计划变化；这些生成数不能当作击杀数或伤害统计。

没有 HCM 的 ERROR 或 WARNING。原版仍有 BOT 导航和资源／退出告警，以及 Buff 触发事件容量不足：98 行相关警告，其中汇总行记录 5068 次额外触发被拒绝。上一局也存在这一问题，汇总数为 6185。这里的触发事件不能直接换算为伤害损失；日志不支持将其认定为 3.6.10 新引入的问题。

因此，可以确认本轮“放开地形、校验门”的修改生效，补投不足显著改善；尚不能确认所有卡怪、所有残留或原版触发容量问题均已解决。继续保留当前诊断，不据这一局宣布全面移除 Debug 的条件已经满足。

原始日志：[打开日志](<C:/Users/GDS-DESKTOP/AppData/Roaming/Fatshark/Darktide/console_logs/console-2026-09-10-20.25.35-534262d9-4361-41db-b7fa-4dddf0fb3446.log:1>)。结构化核对：[审计数据](<F:/SteamLibrary/steamapps/common/Warhammer 40,000 DARKTIDE/mod-work/havoc-redesign-20260905/projects/HavocConditionManager/build/checks/hcm-3610-gameplay-0438-audit.json>)。
