# 3.6.9 最新实测验收：尚未满足移除全部 Debug 的条件

日志：console-2026-09-10-19.52.47-4b3b48a0-6b91-4982-9277-6670bb8e03b9.log。
加载版本：3.6.9。
任务：法庭密牢，2026 年 9 月 11 日北京时间约 03:53:41–04:06:09，结果为胜利；04:06:16 输出最终回执。

## 判断

回收吞吐和额度保留已体现出预期效果；补投仍有较多余额，未接战后方散怪规则本局也没有实际触发。因此只作部分验收，不移除 Debug、不生成去 Debug 的正式包。日志不能证明全图所有残留敌人均已消失，也没有帧耗时证据。

## 核心结果

| 指标 | 上一局 3.6.7 | 本局 3.6.9 |
| --- | ---: | ---: |
| 实际回收 | 104 | 225 |
| 有效补投额度 | 103 | 210 |
| 实际补回 | 29 | 119 |
| 补回／有效额度 | 28.2% | 56.7% |
| 结束时待补额度 | 48 | 91 |
| 过期或容量丢弃 | 26（均为过期） | 0 |

两局时长、敌人总量与配置不完全相同，上表只是各自结果，不能据此声称性能或处理效率提高固定倍数。本局 210 = 119 + 91，已接受额度没有丢失；225 - 210 = 15 表示有 15 个回收对象本身就是补投单位，按规则没有产生第二份额度。再次回收不等于可以确认它们出生点错误，但说明“成功创建”不保证此后一直保持接战。

本局成功创建普通兵 2591、精英 1298、特感 447、怪物 23、其他 4，总计 4363；普通兵与精英跟踪数为 3889。它们都是生成数，不能当作击杀数。

## 已改善的部分

- 就绪队列峰值 31/64，满队列样本 0，结束时待处理引用 0。成功回收对象在基本 30 秒之外的最大额外等待为 11.7 秒，旧版大批远处敌人等待名额的问题本局没有再次出现满队列证据。
- 待补额度峰值 98/256，容量丢弃 0。已完成补投批次的最大年龄为 190.2 秒，说明超过旧 180 秒期限后仍可补回。
- 附近路线通过 23 次，局部导航采样 552 次；生成前检查持续生效。旋转楼梯前方 (37.9879, -86.1169, 16.1507) 多次被判为无路，未把这些失败候选当作生成许可。
- 未发现 HCM 的 ERROR 记录，任务正常胜利结束。

## 尚未完成的部分

210 个额度仍有 91 个、约 43.3% 未能在任务内补入，不能将补投问题视为已经解决。补投阻塞计数为：

| 原因 | 采样次数 |
| --- | ---: |
| 原版对应类别调度暂不允许 | 865 |
| 没有可用位置 | 155 |
| 等待寻路结果 | 154 |
| 在场或中央队列容量限制 | 61 |
| 旧路线失效 | 6 |
| 当前位置不满足条件 | 3 |
| 展开位置问题 | 2 |
| 等待已有请求出队 | 1 |

这些是采样次数，不是秒数、敌人数或互相可比较的阻塞时长。不能直接据 865 次判断原版暂停是故障，也不能从总计数确定那 91 个额度逐一被什么条件挡住。

共用选点进行了 143 次路线查询，其中 85 次包含特殊导航连接而被保守拒绝，21 次无路。该统计同时覆盖额外巡逻、遭遇后补和回收补投，不能把全部拒绝次数都归到回收队列。当前“任何特殊导航连接均拒绝”的规则仍会排除正常可通过的门或跳落，因此它是后续需要评估的候选范围限制，但不应直接放开所有这类连接。

额外遭遇结束时还有四队储备巡逻未完成，实际排队中的三组巡逻还差 86 个成员；这与 91 个回收补投额度是两套不同的队列，不能相加当作同一种残留敌人数。储备巡逻批次 42 实际生成 36 人、其中 26 人后来被 HCM 回收；现有日志没有每个成员的全程轨迹，无法断定是未能接敌还是随后再次掉队。

未接战后方散怪回收数为 0。本局只有三次 passive_not_behind 原因变化，说明存在未满足后方条件的被动散怪；这不证明新规则错误，也不足以作为已在实机触发的证据。

20 条有限回收诊断中，19 条是距最近队员不足 35 米，另 1 条是最终即时视线可见。隔层敌人也可能因直线距离较近而受到保留，这是现行规则的结果。它们不能当作“全图无残留”的证据。

## 原版错误记录

旋转楼梯附近有两条原版 MINION STUCK DESPAWN，均为步枪兵，时间为 03:58:44 和 03:58:48；日志也有原版 BOT 导航、主题资源和其他事件回调警告。两条原版强制移除缺少 HCM 单位或批次编号，不能断定它们就是 HCM 补投单位，亦不能用“无 HCM ERROR”掩盖这些原版异常。

## 本次处理

仅保存验收分析，未修改运行源码、安装目录或已发布 ZIP。因尚不能确认补投和残留问题全面达到要求，本次保留全部现有 Debug，不生成移除 Debug 的版本。

后续应进一步区分调度暂停原因与待补额度类别，并核查能否在保持出生前通行验证的前提下识别正常可用的特殊连接。最终移除时应同时清除调试输出、专用计数、采样状态和对应文档，保留功能必要的队列与原版错误处理。
